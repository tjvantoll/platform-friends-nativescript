<Page xmlns="http://www.nativescript.org/tns.xsd" loaded="pageLoaded">
    <GridLayout rows="auto, *">
        <GridLayout cssClass="header-container">
            <GridLayout tap="logout">
                <Image src="~/app/res/logout_new.png" cssClass="header-button align-left rotateLeftx180"/>
            </GridLayout>
            <Label text="Activities" cssClass="header-context-label" />
        </GridLayout>
        <ListView items="{{ activities }}" itemTap="onActivityTap" row="1">
            <ListView.itemTemplate>
                <StackLayout orientation="horizontal">
                    <StackLayout>
                        <Image src="{{ AvatarUri || '~/app/res/avatar.png' }}" stretch="fill" cssClass="activity-avatar"/>
                    </StackLayout>
                    <StackLayout cssClass="activity-container">
                        <Label cssClass="activity-name-android">
                            <Label.formattedText>
                                <FormattedString>
                                  <FormattedString.spans>
                                    <Span text="{{ UserName }}" fontAttributes="Bold" />
                                  </FormattedString.spans>
                                </FormattedString>
                              </Label.formattedText>
                        </Label>
                        <Label text="{{ Text || '-' }}" cssClass="activity-summary-android" textWrap="true" />
                        <Image src="{{ Picture || '' }}" stretch="fill" cssClass="activity-picture"/>
                        <Label text="{{ CreatedAt, CreatedAt | dateConverter('DD.MM.YYYY') }}" cssClass="activity-date-android" />
                  </StackLayout>
                </StackLayout> 
            </ListView.itemTemplate>  
        </ListView>
<!--             <ActivityIndicator busy="{{ isLoading }}" cssClass="activity-indicator" /> -->
    <AbsoluteLayout cssClass="fabContainer" style.visibility="true" row="2">
      <Image src="~/app/res/add.png" tap="" stretch="fill" cssClass="fab" />
    </AbsoluteLayout>
    </GridLayout>
</Page>
